This kit's input and output are folder trees, not single files, so this
example uses `input/` and `output/` directories instead of `input.*` /
`output.*`. Entirely synthetic — no real Downloads content. `input/` is
an 8-file messy folder; `output/` is what SKILL.md's steps produce from
it; `log.md` is the decision log the kit exports (the `Exports:` line in
../README.md) — the artifact another agent or the person themself would
actually consume, not just the sorted files.
