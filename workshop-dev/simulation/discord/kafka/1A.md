# 1A — Beginner clinic, Mon Sep 21 2026, 16:30–17:00 UTC, #kafka. Facilitator notes (not recorded)

Lead: Rafa. Sachin in #kitcraft. Present in voice: Ingrid, Kwame, Marisol. Aiko stayed until 16:33 (01:33 JST), got the async version verbally, left; written version posted at 17:00. Their own accounts: `1A-floor/<handle>.md`.

## Who came and why

| Person | Blocker at 16:30 | Root cause |
|---|---|---|
| Ingrid | Harness stops at "run `gh auth setup-git`"; `gh` not installed; inventory made in the web editor, no Log line | Personal Mac, Claude Code (paid Pro herself), no package manager, no `gh`; nothing in the pre-work says how to get `gh` on a Mac |
| Kwame | S1 pushes came from a stray second clone in `~`; the Wednesday clone in `~/Documents` is 8 commits behind and has no S1 log line | Block A step 1 cloned again because Codex was opened outside the clone; step 2 then said "cd workshop-kitkraft" without a path, and he cd'd into the new one |
| Marisol | Six 403s on push; Sachin pushed for her twice; wants tonight's push to be hers and wants to see what `gh auth setup-git` changes before running it | Two GitHub accounts in `gh`; the active one has no write access; the block's failure table has "auth prompt" but a 403 is a refusal, so it stopped correctly and she stopped too |
| Aiko (async) | Claude Code installed on the studio Mac, stops at the login screen; no Claude account; inventory hand-written, committed by Sachin | Paid plan needed; a partner decides in her JST morning |

## Minute by minute

- **16:30** Round: one sentence each. Kwame and Marisol can self-drive with me answering; Ingrid needs hands-on. I take Ingrid in voice, answer the other two between Homebrew's progress bars.
- **16:31 Ingrid.** `gh --version` → not found. `brew --version` → not found. Git exists (her harness cloned on Sunday, so the Xcode tools are there). I read the Homebrew one-liner from brew.sh; she types it. It asks for her Mac password; I say "that's your laptop login, type it, nothing shows". She hesitates, types it. Install runs to ~16:37.
- **16:32 Aiko** (voice, before she drops): "You have one blocker and it isn't technical: Claude Code needs a Claude account. When it logs in, do the seven checks in the clinic block; I'll write it in the channel at 17:00 with what 'done' looks like." She leaves at 16:33.
- **16:33 Kwame.** Told him: "one clone per person; find them all first." He runs `find ~ -maxdepth 3 -type d -name 'workshop-kitkraft*'` → `~/workshop-kitkraft` (stray, Wednesday's Block A) and `~/Documents/workshop-kitkraft` (original). For each: `git -C <path> status -sb` and `git -C <path> log --oneline -1`. Stray: up to date with origin, has his S1 commits. Original: `behind 8`, clean, nothing unpushed (`git log origin/simulation/dry-run-01..HEAD` empty). Ruling: the clone that pushed is the real one. Rename the original to `workshop-kitkraft-old` (nothing is deleted; delete after Tuesday if he wants). He reopens Codex inside `~/workshop-kitkraft`, runs check 5: it quotes the autolog line format from AGENTS.md. Good.
- **16:34 Marisol.** `gh auth status` → two accounts, active is the wrong one. `gh auth switch --user marisol`. Before `setup-git` she asks what it writes. I say: "one line in your global git config, credential helper = gh. Look before and after." She runs `git config --global --list | grep credential` (empty), then `gh auth setup-git`, then again: `credential.https://github.com.helper=!/opt/homebrew/bin/gh auth git-credential`. She is satisfied. Note for the block: put this sentence in it.
- **16:37 Ingrid.** Homebrew done; it prints two "Next steps" lines to add brew to PATH. She types both. `brew install gh` runs to ~16:40.
- **16:38 Kwame** runs check 6 in the real clone: `git pull --rebase` (already current), appends the 1A log line, check 7 pushes on the first try. Done at 16:40. He asks whether the old clone's missing S1 line matters. No: the branch is the record, the clone is a copy.
- **16:40 Ingrid.** `gh auth login` → GitHub.com, HTTPS, "Login with a web browser". A code appears; browser opens; she is signed into GitHub in the browser already; approves. `gh auth status` shows her. `gh auth setup-git`. Total from 16:31: 12 minutes, most of it Homebrew.
- **16:42 Marisol** runs checks 6 and 7 herself: log line appended, commit, `git pull --rebase`, push. Push accepted under her account at 16:44. Her first own push. She checks it in a logged-out browser window, as she does.
- **16:44 Ingrid.** Which folder did her harness create on Sunday? `find ~ -maxdepth 3 -type d -name 'workshop-kitkraft*'` → one, in `~`. `cd ~/workshop-kitkraft`, open Claude Code there. Check 5: it quotes the autolog format. First time her harness has read AGENTS.md from inside the clone.
- **16:47 Ingrid, the web-editor ruling.** Her `inventory/ingrid.md` was made with GitHub's "Create new file" (commit "Create ingrid.md"), six headings, her words, no Log line. Ruling, said aloud and recorded here: **a hand-made file counts as the inventory; it does not count as the autolog.** The first harness-written line in it is the proof the harness is wired in. Nothing to redo. She asks the harness for check 6: `git pull --rebase` brings her web-made file into the clone; it adds the missing `## Log` section and the line `… | S1 | ingrid | progress | harness opened inside clone at 1A`. Check 7: commit `S1: ingrid: 1A push`, pull, push. Accepted 16:50. She reads the GitHub page and sees her own commit under her name for the first time.
- **16:51 Ingrid's confession** (read two inventories in the browser before seeing the rule). Ruling: no harm done, the rule exists so the agent does the reading, not to police anyone. Ask the harness to append one `memory` line saying so, so the record is honest. She does. Commit, push, 16:53.
- **16:53** Her question from Sunday: files live on the work laptop, harness on the personal one. Ruling: the kit's examples must be synthetic or anonymised anyway; copy two anonymised files across tonight; nothing real goes in the repo.
- **16:55** Status round (table below). Kwame asks whether "the clone that pushed" rule should be in the block. Yes; observation.
- **16:58** Aiko's async instruction drafted; posted in #kitcraft at 17:00 with the S2 readiness line.

## What the checklist did not cover
- No package manager on a Mac: Homebrew first, 5–8 min plus the Mac password moment. Not in the block; the block assumes `brew` exists.
- Two `gh` accounts; the failure table says "auth prompt" and never "403 / wrong account". `gh auth switch` is not mentioned anywhere.
- A harness outside the clone creates a second clone (Block A step 1) and the hand-off says "cd workshop-kitkraft" without a path. The block needs "print the absolute path" and "do not clone if one exists".
- The folder-name precondition (`…ends in workshop-kitkraft`) fails for anyone who renamed the folder; four of four in S1. Check the remote instead.
- Web-editor file without a Log section: check 6 handled it, but only because I had written the Ingrid case into it on Friday. The ruling above should be in the block.
- Personal vs work laptop split, and "Claude Code needs a paid Claude account": both pre-work problems that surfaced here.
- What `gh auth setup-git` writes; a hypervigilant participant will not run it blind.

## State at 17:00

| Person | Harness | Clone (one, correct remote) | Log line by harness | Own push | Ready for S2 |
|---|---|---|---|---|---|
| Ingrid | Claude Code, personal Mac | yes, `~/workshop-kitkraft` | yes (16:50) | yes (16:50, 16:53) | yes |
| Dev | Claude Code | yes (renamed folder; fine) | yes (S1) | yes (pre-work, S1) | yes |
| Marisol | Codex | yes (renamed folder; fine) | yes (S1, 1A) | yes, own account (16:44) | yes; kit is a stub |
| Tomás | Claude Code | yes (folder `tomasr`) | yes (S1) | yes | yes |
| Aiko | Claude Code installed, no account | no clone | no | no | no; async 1A when the plan exists, then Async 1; her triad reads her kit when it lands |
| Kwame | Codex | yes, `~/workshop-kitkraft`; old clone renamed `-old` | yes (S1 from stray, 1A from real) | yes (16:40) | yes |

Five of six ready; the sixth is blocked on a purchase, not on us. Thirty minutes was not enough for three people if one needs Homebrew: Ingrid took 22 of the 30 and only fitted because Kwame and Marisol self-served. With two Ingrids it fails.
