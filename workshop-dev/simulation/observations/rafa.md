# Observations — Rafa (lead facilitator), prep phase, dry run 01

Severity as in `../OBSERVATIONS.md`: **S1** breaks the session · **S2** costs >10 min or confuses people · **S3** cosmetic. "when" = where I hit it. Rows 1–8 of `../OBSERVATIONS.md` are not repeated unless I found more.

| # | when | sev | what | proposed fix |
|---|---|---|---|---|
| 1 | setup | S1 | `simulation/dry-run-01` did not exist on the remote when I cloned (`fatal: Remote branch not found`); only `main` existed. I cloned `main` and created the branch locally; the coordinator pushed it minutes later and my first push rebased onto it cleanly. A participant agent following the brief literally would have stopped. | Create and push the branch before any agent is briefed; the brief's clone command should fail loudly with a "wait for the branch" message, not a git error. |
| 2 | setup | S2 | `workshop-dev/simulation/README.md` and `OBSERVATIONS.md` were also absent at clone time (arrived with the branch). Reading list in the brief named files that did not exist. | Same as 1. |
| 3 | push procedure | S2 | The prescribed push sequence (`pull --rebase origin simulation/dry-run-01` → push) fails when the remote branch is absent; I had to add `push -u`. `AGENTS.md` for participants has the same shape and the same hole. | Procedure: `git fetch; git rev-parse --verify origin/<branch>` first; if absent, `push -u`. |
| 4 | push procedure | S3 | `gh auth token --user rafaeldavid` worked, but the machine's active `gh` account is a different one (`rafathebuilder-ZK`). Nothing in the brief says which account has write access to `protocolvision/`. | State the GitHub account per facilitator in `prep-checklist.md`. |
| 5 | pre-work email | S1 | The scaffold never says how a participant authenticates a `git push` over HTTPS. Non-technical registrants (Ingrid, Aiko, Marisol) will hit a password prompt and stop. The 1A budget cannot absorb three people doing `gh auth login` cold. | Add to the email: install `gh`, run `gh auth login` then `gh auth setup-git`. Put it in `README.md` step 2. |
| 6 | pre-work email | S1 | `workshop-kitkraft-private` is referenced by README, outline §1, pins, prep-checklist (Sep 12, owner Rafa) and the email draft, and it does not exist. Marisol's kit depends on it. | Decided at checkpoint 1: dropped. Content stays on the participant's machine, stub goes public, bridge through the owner's agent. Email, S1, S3 rewritten. |
| 7 | pre-work email | S2 | Draft has `<time>`, `(link)` for the survey, and no "no GitHub account" path even though `registration/README.md` says the email covers it. | Filled in; survey URL is in `resources.md` §0. |
| 8 | pre-work email | S2 | The email asks for a test push but the outline gives the registrant no concrete first push. Sep 19 "push check" has nothing to check. | Email asks for `inventory/<name>.md` with just a title, pushed; that pre-creates the S1 file. |
| 9 | registration | S2 | `registration/README.md` Q3 says invites go to `kitcraft-2026`; the repo is `workshop-kitkraft`. | Fix the name. |
| 10 | outline v7 | S1 | S1 is written for 90 min (sums to 85); the slot is 60. README also says 90. | Slides cut as in `S1-slides.md` header; update outline and README. |
| 11 | outline v7 | S1 | Outline puts S2 Tuesday morning; the schedule has S2 Monday 20:00 UTC. Async 1 becomes 17:00–20:00 (3 h, with 1A inside it for three people). | Async 1 = README.md only, two paragraphs. Async 2 becomes overnight and can absorb more. |
| 12 | outline v7 | S2 | Roles: outline says "roles swap per session"; the facilitator briefs say Rafa leads S1/S3 and Sachin leads S2/S4, with the other on chat/repo. Both readings are consistent, but no file states who is lead in which session. | Decided at checkpoint 1: sections split by expertise, Sachin opens; Presenter column added to both run-of-shows and `(presenter: …)` on every slide. |
| 13 | outline v7 | S2 | Breakout rooms are `kitcraft-1…7` in the outline and pins; the rehearsal uses `breakout-1`, `breakout-2`. | Pick one name set; put it in the pin. |
| 14 | outline v7 | S1 | S3 target list (C3PO MCP, Prime Radiant, water registry) is mostly unavailable: no live services in the rehearsal, Prime Radiant "TBC", water registry "TBC". `facilitator/` had no kit folders at all ("each target gets a kit folder here" — none existed). | Facilitator kits are the real targets. I added `facilitator/rafa-kit/` and a row in `facilitator/README.md`; `sachin-kit/` is still missing at prep time. |
| 15 | outline v7 | S2 | The S1 exercise fallback is a "portal upload form"; no portal exists (also `../OBSERVATIONS.md` #8). Discord pin has `Portal: <url>`. | Fallback is "send the file to Sachin in #kitcraft". Remove portal mentions. |
| 16 | outline v7 | S2 | Triads: S3 rotation "one step so nobody bridges to the kit they critiqued" is defined for pairs only. `bridges/<a>-<b>.md` "one log per pair" has no triad meaning. | Rule in `S3-slides.md` slide 9: critique ring X→Y→Z→X, bridges X→Z, Y→X, Z→Y; one log per bridger, single writer. Assumed the S2 ring; Sachin must confirm. |
| 17 | outline v7 | S3 | Ice-breaker seeds "Wobblebots, paintings articulation" appear nowhere else with a description (resources §7 lists them as "candidates without URLs"). I could not use them. | Either one sentence each in resources or drop them; I seeded with the water-rate rule instead. |
| 18 | outline v7 | S2 | Awareness-grid axes are flagged "confirm before the slide is made"; not confirmed. | Confirmed at checkpoint 1: External/Internal × Archive/Live. Flag removed. |
| 19 | consent | S2 | Consent notice must say where material goes after Tuesday (outline §5, §7: decide before Sep 19); nothing is decided. | Decided at checkpoint 1: public repo, synthesis in Discord within 48 h, nothing else. Slide 2 updated; no C3PO wording. |
| 20 | AGENTS.md | S1 | Rehearsal only: `AGENTS.md` tells every harness to `pull --rebase origin main` and push to `main`. Participant agents following it in the dry run will write to `main`, which the rehearsal forbids. | For the rehearsal, either a branch-aware `AGENTS.md` on this branch or each participant clone checks out the branch explicitly (added to 1A step 3). |
| 21 | AGENTS.md | S2 | Removal right ("ask a facilitator to remove any line") vs. "never rewrite or delete earlier lines" for harnesses. Consistent only if facilitators delete by hand; nobody is told that. | One sentence in `facilitator/README.md`: facilitators remove lines by hand on request and note it in the transcript folder. |
| 22 | recorder | S2 | S3 has a 36-min breakout: #kafka empties, recorder auto-stops or is stopped, then `/record` runs again with the same name `KITCRAFT-S3`. Unknown whether OpenRecapper accepts a duplicate name; `RECORD_MEETING_NAMES` lists S1–S4 only. | Add `KITCRAFT-S3-2` (and `-S2-2`) to the meeting names, or confirm duplicates are fine, in the Sep 17 dry run. |
| 23 | recorder | S3 | Outline says `/record channel: name:`; brief says `/record channel:#kafka name:KITCRAFT-S1`. The second is what I used; the outline should carry the exact syntax. | Put the exact commands in the pin. |
| 24 | inventory | S3 | "Ask your agent to find the two entries closest to yours" needs the agent to pull first; nothing says so. With 6 people the result is 2 of 5. | Prompt in slide 13 says "pull the latest repo" first. Fine for 6; at 15 it needs a minute more. |
| 25 | rafa-kit | S2 | "Water-rate PDF → registry row" has no published registry schema anywhere (resources §5: "interface to be specified"). I invented 15 columns, long format, one row per class × meter × tier. | Rafa confirms the column set or points at the real registry file; logged as a `question` line in `facilitator/rafa-kit/LOG.md`. |
| 26 | KIT_TEMPLATE | S3 | `examples/README.md` asks for `input.*` and `output.*` but the S2 build gives 35 min; a PDF input cannot be committed as a "real" input by non-technical people. Synthetic text extracts are the realistic case. | Say "text extract or synthetic input is fine" in the template and in S2. |
| 27 | personas | S2 | Aiko is absent from S2 but present in S3; her kit lands at 00:00 UTC Tue. Nothing in the outline handles a kit that was never critiqued. | S3 slide 2 says it counts; her bridger (Kwame) will be the first agent to read it cold, which doubles as the critique. |
| 28 | S2 timing | S2 | S2 and S4 are 22:00–23:00 Berlin. Fine for the rehearsal; for the real cohort this will lose Europeans. Not mine to fix. | Note for the organiser. |
| 29 | 1A | S2 | The outline gives 1A no content beyond "install and first push". Three of six need it. The 30 min holds only if `gh auth setup-git` is done in pre-work. | `1A-clinic.md`; email item 2. |
| 30 | facilitator/README | S3 | `AGENTS.md` forbids harnesses from editing `facilitator/`; my own harness had to edit it to add the kit row. Facilitator agents need an explicit exemption on the simulation branch. | One line in `simulation/README.md`: facilitator agents may edit `facilitator/` and `workshop-dev/simulation/`. |
| 31 | S3 reconcile | S2 | Lead and co-facilitator wrote S3 timings independently and got different breakout windows (mine 15:48–16:24, Sachin's 15:45–16:25). Nothing in the scaffold fixes the breakout clock before both write. Same risk in S2. | Sachin's window wins (he moves the rooms); I retimed. Put the breakout clock for all sessions in one shared table before anyone writes slides. |
| 32 | S3 reconcile | S3 | My slide-2 consent assumption ("nothing decided after Tuesday") was superseded by Sachin's `consent-notice.md` (48-h post, possible C3PO ingest announced first). Two versions of the same fact existed for an hour. | Consent text has one owner (Sachin); slides point at his file. Done. |
| 33 | S3 reconcile | S3 | The triad rotation I derived from the personas matched Sachin's `breakout-instructions-triads.md` exactly, without coordination. Good sign, but it was luck: the outline's pair rule had to be reinterpreted twice. | Put the triad rule (A reads B, B reads C, C reads A; bridges reversed) into `outline.md` §4. |
| 34 | checkpoint 1 | S2 | The presenter split by expertise means the "lead" of a session speaks for about half of it; the run-of-show's Lead/Co-facilitator columns and the Presenter column now describe different things (who is on mic vs. who owns the activity). Easy to misread on the day. | Keep both, but the Presenter column is the one to rehearse from; say so in the header (done). |
| 35 | checkpoint 1 | S3 | `KIT_TEMPLATE/README.md` on the branch still says "content lives in workshop-kitkraft-private" and has no `Interface:` line; Sachin's template change had not landed when I applied the decision. | Sachin updates the template; my README carries `Interface:` already. |

Assumptions I made (all marked in the files)
- Awareness-grid axes: External/Internal × Archive/Live (confirmed, checkpoint 1).
- S2 critique ring is the forward ring within each triad; S3 bridges reverse it (confirmed by Sachin's file).
- After-Tuesday disposition: repo stays public, synthesis in Discord within 48 h, nothing else (confirmed, checkpoint 1).
- Registry row schema for `rafa-kit` (15 columns, long format).
- Recorder is Sachin's to run in every session; 1A is not recorded.
