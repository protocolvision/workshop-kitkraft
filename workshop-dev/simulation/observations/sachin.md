# Observations — Sachin (co-facilitator agent), dry run 01, prep phase

Every point where the scaffold or outline lacked something I needed, contradicted itself, or forced a guess. "Assumed" = what I did without a decision from Rafa. Severity: S1 breaks a session · S2 costs >10 min or confuses people · S3 cosmetic. Cross-refs to `../OBSERVATIONS.md` as OBS #n.

| # | when | sev | what | proposed fix |
|---|---|---|---|---|
| 1 | setup | S1 | `simulation/dry-run-01` was not on GitHub when I was told to clone it; the briefed clone failed. Found the branch in the local source repo and fetched it from there; Rafa's clone was based on `main` *without* the scaffold commit, so the two facilitator clones started from different bases. Coordinator pushed the branch ~20 min later; my rebase onto it was clean, but only by luck. (OBS #9 covers the incident; this row is the divergent-base part.) | Briefing includes a `git ls-remote --heads <repo>` line and is not sent until it shows the branch; all clones from the remote, never from a local path. |
| 2 | setup | S2 | `AGENTS.md` forbids editing `facilitator/`, `transcripts/`, `workshop-dev/` and non-party `bridges/` files — but the co-facilitator's harness must write all four (`facilitator/sachin-kit/`, transcripts after each `/stop`, `bridges/SYNTHESIS.md`). A compliant harness refuses. Assumed: facilitators are exempt. | Add a `## Facilitators` clause to `AGENTS.md` (who, which paths, commit prefixes `prep:`/`S<n>: <facilitator>:`), or a `facilitator/AGENTS.md` override. |
| 3 | setup | S1 | Participant-facing `README.md` session table says S1 is 90 min and S2/S3/S4 are all Tuesday; the outline header agrees. The fixed schedule has S2 on Monday 20:00. Anyone reading the README misses S2. I posted a correction in #kitcraft (Mon 08:10). | Make the pre-work email table the single source; update README and outline header before Sep 15. |
| 4 | setup | S2 | `discord-pins.md` is a placeholder: portal `<url>` (no portal), a private-repo link (repo does not exist), times TBD, "pairs", no survey link, no room names, no help path. Rewrote from scratch. | Replace the file with the pins in `simulation/discord/kitcraft.md`; delete the portal line (OBS #8). |
| 5 | setup | S2 | Breakout room names: outline §3 and prep checklist say `kitcraft-1…7`; fixed facts and the sent pre-work email say `breakout-1`, `breakout-2`. Used the latter. | Align outline §3 and the Sep 14 checklist row. |
| 6 | setup | S3 | Discord caps a message at 2000 chars; the main pin is ~1900 with no room for triads. Split into four pins (main, consent, S2 triads, S3 triads). | Outline §3: "pinned messages", plural, and say which four. |
| 7 | setup | S2 | "Adjacency pairs from inventories, overnight" is impossible: S1 ends 16:30, S2 starts 20:00. Assumed: provisional triads from registration Q5/Q6 posted Monday 08:05, confirmed at S1 close, pinned 16:45. | Outline §4 and checklist Sep 21: triads drafted from the form on Sep 19, confirmed after S1. |
| 8 | setup | S2 | Critiquing a stub kit (marisol) is degenerate — nothing to run. No rule anywhere. Assumed: the reader's agent reports "what I would need from the owner"; assigned devr, the most capable reader. Who hits the stub is a facilitator decision the outline does not name. | Stub variant in the breakout instructions (done); say in the outline that the stub goes to the strongest reader. |
| 9 | setup | S2 | Bridging to a stub: the outline names the "transmittal pattern" (Rao–Jenna) but gives no procedure. Wrote one: requester appends a request to the bridge log and pushes; owner runs locally, appends the output, pushes; alternate pushes. | Put the procedure in `bridges/README.md`. |
| 10 | setup | S2 | A triad losing a member (aiko, S2 and S4): triads are the outline's fallback for drops, but nothing covers a triad dropping to a pair. Assumed: the two present critique each other; the absent kit is read async and the report posted in #kitcraft; her S4 paragraph is read by a triad-mate. | Absence rule in the breakout instructions (done) and outline §4. |
| 11 | setup | S2 | OBS #7 (two harnesses on one pair log) disappears with triads: a rotated cycle gives each person one distinct target, so one log per bridge, one writer, `bridges/<you>-<target>.md`. That is six logs, not "one per pair", and S4's "each pair 90 s" becomes 5 speakers + 1 read-out = 12 min. | `bridges/README.md` and `TEMPLATE.md`: "one log per bridge, one writer"; outline S4 arithmetic per triad. |
| 12 | setup | S1 | OpenRecapper auto-stops on an empty channel and after 20 min of silence. Outline says "re-run `/record` after breakouts" but not to `/stop` before; unknown whether a second `/record` with the same `name:` appends or overwrites. The S1 exercise is 25 silent minutes. Assumed: explicit `/stop` before every breakout; reuse `KITCRAFT-S<n>`; a facilitator speaks every ≤15 min. | Runbook rules (done); Sep 17 dry-run item: test a second `/record` with the same name; fallback names `KITCRAFT-S2B/S3B` in `RECORD_MEETING_NAMES`. |
| 13 | setup | S2 | S4 synthesis "reads the live transcript", but the transcript file exists only after `/stop` at 21:00, and the live text is in a Discord channel my agent cannot read. Assumed: paste the channel text into a scratch file at 20:24; SYNTHESIS v1 from repo files, v2 with the transcript within 48 h. | Outline S4 and §5: two-version synthesis; name the paste step. |
| 14 | setup | S2 | Awareness-check answers from S1–S3 have no home in the repo; the S4 profile needs them. Assumed: `transcripts/S<n>/awareness-check.md`, written by the co-facilitator at each close. | Add to outline §5 and `transcripts/README.md`. |
| 15 | setup | S2 | Transcript copying: outline says "copy after each `/stop`", owner "both"; no commit prefix for facilitator commits (`AGENTS.md` lists only participant prefixes; the brief gave me `prep:`). Assumed: Sachin within 30 min; `S<n>: sachin: transcript + summary`. | Name the owner in §5; add facilitator prefixes to `AGENTS.md`. |
| 16 | setup | S2 | Consent promise "remove any line" is true only at HEAD; git history keeps it unless a facilitator force-pushes, which OBS #5's ruleset would block. Also §7's post-workshop minimum ("decide before Sep 19 so the consent notice can say where the material goes") is undecided. Assumed: notice states the history caveat; C3PO ingest "announced first"; ruleset gets a facilitator bypass. | Decide §7 by Sep 19; ruleset: block force-push for everyone except facilitators; adopt the consent text. |
| 17 | setup | S3 | Consent text in outline/README lacked: live stream to #kitcraft-transcripts visible to all server members; AI summary via relay; opt-out-without-leaving; that 1A is recorded. Added all four. | Adopt `consent-notice.md` as canonical; link it from README §Consent. |
| 18 | setup | S3 | Three identities per person: form name, GitHub username, Discord display name (in the diarized transcript). Folder names and transcript names will not match; the synthesis needs a map. Sim uses persona handles everywhere, which hides this. | Facilitator-only roster (not in the repo); ask participants to set Discord display name = folder name for two days. |
| 19 | setup | S2 | `facilitator/README.md` lists six targets, but only `sachin-kit` and the brand kit are repo-readable kits: `ask_c3po` needs a token, Prime Radiant and water are "TBC", and "each target gets a kit folder here" is unfulfilled except mine. No live services in the rehearsal anyway. Assumed: two alternative targets. | Write the folders by Sep 13 or shrink the table to what exists. |
| 20 | setup | S2 | S2 timing: examples 15 + build 35 (critique "at minute 25") + close 10 = 60 leaves zero time for room moves (~2 min each way). Assumed: build 20, critique 13 including moves, close 10, 2 min slack; the comms layer is the last 5 min of the build. | Outline S2 with the compressed numbers. |
| 21 | setup | S3 | Brand-kit example "James if available; else Sachin" — nobody confirmed. Assumed Sachin. Also OBS #1 and #2 fixes conflict: #2 moves the Async 1 README draft into the S1 tail, #1 shrinks S1 to 60 with no tail. Assumed Async 1 stays as the 3-hour gap. | Pick one; I recommend the gap (people need the break more than the tail). |
| 22 | setup | S3 | `registration/README.md` says invites go to `kitcraft-2026`; the repo is `workshop-kitkraft`. | Rename. |
| 23 | setup | S2 | The sent pre-work email promises a private-repo invite "before Monday" to anyone replying "private"; the repo does not exist on Sep 20. Marisol is affected. Posted the workaround (stub + transmittal) in #kitcraft. | Create the repo by Sep 15 or drop the promise from the email. |
| 24 | setup | S3 | The channel-log format given (`HH:MM UTC — @handle`) has no date; two days of messages need one. Added `### day` headers. | State the format, with day headers, in `simulation/README.md`. |
| 25 | setup | S3 | `KIT_TEMPLATE/README.md` has no line for the kit's interface (ladder rung), though S2 teaches it and `bridges/TEMPLATE.md` asks "Interface used". Added `Interface:` to my kit README. | Add `Interface:` to `KIT_TEMPLATE/README.md`. |
| 26 | setup | S2 | Hypothesis test in S4: "where the logs support it, say so" has no decision rule; with six paragraphs and a room of five the read-back looks rigged without one. Wrote one (≥4 support and 0 contradict → retained; supports ≥ contradicts naming one element → amended; else rejected; ties to the room). | Adopt or replace in outline S4. |
| 27 | setup | S3 | `git pull --rebase` + `gh auth token --user rafaeldavid` + credential-helper push worked first time; both facilitator agents push as `rafaeldavid`, so GitHub attributes every commit to one account and the sim's "who pushed what" is only in the author field. | Acceptable for the rehearsal; note it in `PROTOCOL.md`. |
| 28 | reconcile | S2 | Lead role per session is read two ways: my brief says Rafa runs every activity and I own chat/rooms/repo (so my S2/S4 run-of-show has Rafa presenting and me on the brand kit, synthesis, hypothesis, tally); Rafa's observation #12 reads "roles swap" as Sachin leading S2 and S4. Both materials exist; the room would see two people expecting the other to talk at 20:00 Monday. | One line in `prep-checklist.md` naming the lead per session; I recommend the brief's reading (Rafa leads, Sachin presents the three named segments), since the recorder, rooms and synthesis already sit with me. |
| 29 | reconcile | S3 | 1A recording: my runbook and consent notice had 1A recorded as an S1 second segment; Rafa's 1A checklist and S3 notes say 1A is not recorded. Two versions of a consent fact for an hour. Aligned mine to "not recorded" (a setup clinic should be off the record). | Recording scope per session in the shared schedule table (`simulation/README.md`), owned by the recorder operator. |
| 30 | checkpoint 1 | S3 | Dropping the private repo meant editing seven files in five folders (README, AGENTS, kits/README, KIT_TEMPLATE, registration, pins, and the sent email, which I could not touch). The idea had no single home. | Decisions that touch participant-facing text get one canonical paragraph (README §How to participate) and the other files link to it. |
| 31 | checkpoint 1 | S2 | The dry-run line added to `AGENTS.md` ("every `main` below means `simulation/dry-run-01`") asks a harness to translate a command; a literal harness runs `git pull --rebase origin main` and drags `main` into the branch. | For the sim, write the branch name into the command itself and swap it back on cherry-pick; or run the sim on a fork whose default branch is the sim branch. |
| 32 | checkpoint 1 | S3 | Presenter split by expertise gives S2 six handovers in 60 min (S→R→S→R→S→S) and S4 five. Each handover on Discord costs a screen-share swap (~30 s). Budgeted in the run-of-shows; not in the outline. | One screen-sharer per session (the co-facilitator shares; the presenter talks), so handovers cost nothing. |
| 33 | checkpoint 1 | S1 | Original `breakout-instructions.md` S2 step 2 quotes a prompt for the partner's kit but never says to **pull** first; the partner's kit is not on disk until you do. A harness executing it literally reports "folder not found". Same in the S3 block. | Every block now starts with commit → pull --rebase. |
| 34 | checkpoint 1 | S2 | Original S2 step 1 "Swap kit folder paths" and step 3 "read the report to your partner" are human actions with no agent counterpart; step 3 does not say which LOG.md or the line format, and nothing says what to do with a stub or an absent partner. | `instructions/S2-critique.md`: target from the pin, exact log line, STUB and ABSENT variants. |
| 35 | checkpoint 1 | S2 | Original S3 step 3 leaves `<one thing my kit cannot do alone>` as a placeholder the participant must invent under time pressure; step 5 gives no commit message, and `bridges/<a>-<b>.md` does not say who is `<a>`. | Block asks for the one thing and refuses to proceed without it; names the writer and the commit message. |
| 36 | checkpoint 1 | S2 | `inventory/README.md` prompts were questions to a human ("Ask your harness to write it from these prompts"), with no file shape, no log line, no pull before "find the two entries closest to mine". | Rewritten as a harness block that writes the file in `_example-rafa.md`'s shape, pulls, then compares. |
| 37 | checkpoint 1 | S2 | Async 1 in the outline: "draft `kits/<name>/README.md` in two paragraphs" contradicts `KIT_TEMPLATE/README.md`, which is a field list; a harness cannot satisfy both. Async 2: "push v2" with no definition of v2, and "facilitators regenerate the portal" (no portal). | `instructions/async-1.md` fills the template fields; `async-2.md` defines v2 as one `progress` line per blocker plus a re-run of the example. |
| 38 | checkpoint 1 | S2 | Consent "ask a facilitator to remove any of your lines" was ambiguous to a harness, which might delete the line itself and break append-only. | AGENTS.md and the consent block: never delete, even when asked; removal is facilitator-only. |
| 39 | checkpoint 1 | S2 | The transmittal pattern had no executable form and needs two different harnesses (requester, owner) acting in turn on one file. | Two role blocks in `bridges/README.md`; alternate-push rule; `### Request` / `### Response` headings in the template. |
| 40 | checkpoint 1 | S3 | Participants never read `workshop-dev/`, so agent blocks had nowhere participant-facing to live; pins cannot hold them (2000 chars). Created `instructions/` and pointed every pin and post at a file there. The breakout-instructions file now carries only the posts; the blocks are not duplicated. | Keep `instructions/` as the single home; the outline's "pre-written instructions in #kitcraft" becomes "pre-written files in `instructions/`, linked from #kitcraft". |
| 41 | checkpoint 1 | S3 | The consent-block-for-harnesses and `AGENTS.md` now say the same rules in two places; drift risk. | Consent notice links to the AGENTS.md section instead of restating it once the wording is final. |
| 42 | S1 close | S1 | Log-line scan vs. what the run-of-show assumed. Assumed: six harness lines. Found: devr, tomasr, marisol fired (marisol's pushed by me); kwame's line is on GitHub but was written from a second clone whose branch is `main`, pushed with `HEAD:simulation/dry-run-01`, so his real clone is behind and its autolog never fired; ingrid has a file with an empty `## Log` (web editor); aiko had no file on the branch at all. Four of six is the true number, not "two missing". | The check must read the branch, not the room; the 1A block's checks 5–6 cover kwame and ingrid; aiko needs the async path. |
| 43 | S1 close | S1 | Two people with no harness (ingrid, aiko) had no executable path in S1: Block 1 assumes a harness. Ingrid used the GitHub web editor (bypasses AGENTS.md, no log line, default commit message, branch chosen by guesswork); Aiko pasted the file in the channel and it sat there for 40 min because the co-facilitator cannot commit pasted files while running the recorder, the thread and Marisol's pushes. Block 2 therefore saw four files, and Rafa told the room "it didn't pull". | A "no harness" variant in `inventory/README.md`: fill the five headings by hand, paste in #kitcraft; the co-facilitator commits at a fixed minute (16:14) announced in the run-of-show, not "as I go". Never say a count in voice without reading the branch. |
| 44 | S1 close | S2 | Triad disagreement. Provisional triads (from the form) had zero mutual adjacencies among the four agents that ran Block 2; the results form a ring devr↔kwame, tomasr↔marisol. Re-cut: T1 ingrid/devr/kwame, T2 tomasr/aiko/marisol. Cost: S2's live critique in T2 is a stub read plus a pair (aiko async); S3's transmittal requester becomes aiko (lightest role, right for her). Rosters had to change in six files. | Decide triads from Block 2 results at a fixed minute after S1, not from the form the morning before; keep rosters in one file the others reference. |
| 45 | S1 close | S1 | Block 1 precondition "folder name ends in `workshop-kitkraft`" stopped three of four harness users (marisol, tomasr, devr); all overrode it on `git remote -v`. Marisol logged it as a decision; the block's own rule sent them to re-clone mid-exercise. Fixed in `instructions/orient.md` and `inventory/README.md`: remote first, folder name a hint. | Rafa's Block 1 (`S1-agent-prompts.md`) and the 1A block check 4/5 still test the folder name; align. |
| 46 | S1 close | S1 | The `(dry run only: simulation/dry-run-01)` parenthesis in the blocks produced three different behaviours: ingrid's harness asked her which branch ("the website says main"), kwame's pushed `HEAD:simulation/dry-run-01` from a `main` clone, marisol logged the question on Sunday and never got an answer. Observation 31 predicted this. | `git branch --show-current` is now the rule in orient.md; for the real day the parenthesis must be gone. |
| 47 | S1 close | S2 | Marisol's push was refused (403) because git on her machine is signed in as a different GitHub account; the block's failure handling only covers "auth prompt". A refusal is not a prompt and she correctly refused to type anything. Sachin pushed for her twice. | Failure line in every block: "403 / permission denied → stop, paste the error, facilitator pushes; do not switch accounts mid-exercise." Add `gh auth status` to the pre-work check. |
| 48 | S1 close | S2 | Ingrid read two inventories in the browser against the rule, because she could run nothing and Rafa told her to "ask a triad-mate" instead; she confessed in voice and in the channel. The rule had no path for people without a harness. | A no-harness participant is told: "your triad-mate's agent reads yours and tells you; you read nothing"; say this in the exercise briefing, not after. |
| 49 | S1 close | S3 | Sachin's transcript lines as pre-written by Rafa said "six in, all six on the branch" at 16:16 and "committing Ingrid's and Aiko's now" at 16:13; both were false on the branch. I corrected my own lines to the truth. A lead writing the co-facilitator's lines in advance will state counts the co-facilitator has not verified. | The lead never scripts the co-facilitator's numbers; the run-of-show says "Sachin reads the branch" and leaves the line blank. |
| 50 | S1 close | S3 | The channel record had no facilitator lines during the session even though the transcript refers to eleven of them ("posted the order", "tally's in the channel", "block is pinned", "thread open"). Added them at their timestamps; the log was also not chronological (participants' agents appended out of order). Sorted it. | The co-facilitator's in-session posts are pre-written in the run-of-show with timestamps and posted as a batch; the log format gets a rule: append in time order or it will be sorted. |
| 51 | S1 close | S2 | Questions the pins did not cover, all asked in the channel: whether the folder check means name or remote; `main` or the branch; whether the `HEAD:` push form is allowed; whether a hand-written log line counts; whether 1A can be async; "the room's six or the branch's four"; do Log lines render as one paragraph on GitHub (yes: no blank line). Answered 16:38–16:44. | Each answer became a change: orient.md precondition, branch rule, 403 rule, no-harness path, PINNED 5 (1A block), blank line after `## Log` in the template. |
| 52 | S1 close | S3 | `awareness-check.md` and the summary both needed the S1 result; the expected winner (precommodification) lost to anchored 3–2–1–0. The S2 slide 1 bullet had a placeholder "N of 6 said precommodification" that assumed the prediction; corrected. | Slides that quote a check result get the value from the file, never a pre-filled expectation. |
| 53 | S1 close | S3 | Committing Aiko's file under `--author="aiko (sim)"` with a `S1: sachin:` message works but the autolog line had to be hand-written by me; the file now carries two hand-written lines (hers and mine), zero harness lines, and Block 2 never ran for her. Her first harness line will come from the 1A block in her morning. | Accept for the rehearsal; note in the synthesis that aiko's S1 record is human-written. |
| 54 | S1 close | S2 | First real git conflict of the rehearsal, on `discord/kitcraft.md`: I re-sorted the Mon Sep 21 section while Ingrid's and Rafa's agents appended 1A posts; `pull --rebase` conflicted in two hunks. My resolution script failed but the chained `git add`/`rebase --continue` ran anyway (a heredoc broke the `&&` chain), so six conflict markers were committed and pushed-attempted before I caught them; fixed one commit later. Any shared append-only file edited by more than one agent will do this; the channel file is the one everyone writes. | One writer per file for anything not append-only; for append-only files, never reorder — append and let a reader sort. Facilitator scripts: check the working tree for markers before any commit (a pre-commit hook: `grep -rl '^<<<<<<<' && exit 1`). |
| 55 | layout | S2 | One `LOG.md` per person leaves two log locations in S3: the bridge file's own `## Log` (template) and `participants/<NAME>/LOG.md`. I kept both (bridge step lines in the bridge file, one summary line per step in LOG.md) and wrote that into AGENTS.md; a harness will still ask which. | Decide one: drop `## Log` from the bridge template, or make LOG.md the only log and let bridge files reference LOG lines. |
| 56 | layout | S1 | "Open your harness inside `participants/<NAME>/`": Claude Code walks up to find `CLAUDE.md` so `@AGENTS.md` still loads; whether Codex finds the root `AGENTS.md` from a subfolder is unverified. Two personas use Codex; on the real day this is minute 0. | Sep 17 dry-run item: open Codex in `participants/test/` and check it states the autolog rule. Fallback: a one-line `AGENTS.md` in `PARTICIPANT_TEMPLATE/` pointing at `../../AGENTS.md`. |
| 57 | layout | S3 | The layout mandate meant editing Rafa's S1-agent-prompts, 1A-clinic, S1/S3 slides and run-of-shows (paths, step 0, LOG.md proof lines) — done and noted here. The as-sent pre-work email got rewritten by the same pass; I reverted it because an as-sent record must not change, so its block now names `inventory/NAME.md`, a path that no longer exists. | One line at the top of `pre-work-email.md` (Rafa's file): "layout changed Sep 21 17:35; see #kitcraft"; or an AGENTS.md redirect ("`inventory/NAME.md` now means `participants/NAME/inventory.md`"). |
| 58 | layout | S2 | Regex migration hazards met: "copy `KIT_TEMPLATE/` to `kits/<name>/`" would have become "…to `participants/<name>/kit/`" (wrong target) without a phrase rule; the step-0 insertion landed inside the consent notice's rules block (removed). | Path constants in one place (`instructions/README.md` now has them) that every block cites, so the next rename is one edit. |
| 59 | cohort | S2 | Four people: the S4 hypothesis threshold moves from ≥4 of 6 to ≥3 of 4; the awareness profile has a six-answer S1 row and four-answer S2–S4 rows, so "which quadrant won" must be read per row with n shown, not as counts across rows. | Synthesis §5 shows n per row; stated in the S4 run-of-show. |
| 60 | cohort | S1 | With pairs, the transmittal (stub) bridge rests entirely on Tomás → Marisol, and Tomás's persona wants Sachin's kit. If he switches, the cohort's only stub is never bridged and the pattern goes untested. Pinned: stub first, `facilitator/sachin/kit/` as hard mode after. | Rule: a stub owner's partner bridges the stub first; facilitator kits are always second targets. |
| 61 | cohort | S3 | Withdrawn participants keep their folders. S1 `memory` lines now point at absent people (devr → kwame; kwame → devr, marisol) and the synthesis reading `participants/*/` will count six inventories and four kits. | Synthesis §1 marks "withdrew after S1, inventory readable"; the two withdrawal posts added to the S4 source list. |
| 62 | cohort | S3 | Sequence: I re-pinned pairs at 18:55 on Kwame's withdrawal alone, which silently dropped Aiko from the pair structure three hours before her 22:10 post; at 18:55 she was still nominally async in pair 2. Recorded as an assumption ("nothing changes for you tonight"). | On the real day a member becomes "async", never silently out; the pin lists who is async, not only who is in a room. |
| 63 | cohort | S3 | Pair 2's critique is asymmetric by design (Tomás reads a stub and reports what he would need; Marisol reads a real kit); pair 1 is symmetric. The S2 close round is four sentences, one about a stub, so the "expected: anchored" check has a thin base. | Accept; say so in the S2 summary. |
| 64 | tooling | S3 | Two scripting errors of mine, recorded because the migration will be redone on `main`: `git mv -q` does not exist (no quiet flag), so a `set -e` loop created empty folders and moved nothing until re-run; a Python `SystemExit` inside a heredoc left the trailing `git commit` running on a half-applied edit set (commit 2 carried only the AGENTS/README semantics). | Ship the migration as a checked-in script (`workshop-dev/migrate-layout.sh`) that asserts every expected path exists before it commits. |
| 65 | root decision | S3 | Reversing "open inside `participants/NAME/`" to "open at the clone root" touched 14 blocks and 7 prose sites, two hours after the first rewrite; nothing surprised me except how much of the harness-facing text encodes *where* the harness sits. Root is the better call (Codex discovery question in obs 56 disappears; the write boundary is a rule, not a cwd). The 17:35 channel post now carries a superseded instruction; I added a 17:50 correction rather than rewrite the record. | Keep "where the harness opens" as one sentence in AGENTS.md and have every block cite it, so the next change is one edit. |
| 66 | root decision | S3 | The `WITHDREW.md` convention was briefed, written, pushed, then withdrawn within the hour in favour of "work with what is in the repo". Because the first commit was already on the branch, the reversal is a second commit (no force-push). The replacement rule is better: it covers no-shows, late pushers and withdrawals with one behaviour, and needs no marker file. | Rule now in AGENTS.md and the S1/S2/S3/S4 blocks; the S4 count is "folders with a LOG.md line from that session". |
| 67 | layout | S3 | Top-level `bridges/` removed; transmittal blocks now live at the end of `instructions/S3-bridge.md` (one file per exercise, as the instructions index promised) and the synthesis under `transcripts/S4/`. Outside `workshop-dev/simulation/`, the only remaining `bridges/` hits are relative paths in the participant folder description and Rafa's v7 `outline.md` / `breakout-instructions.md`, which I did not rewrite (they are the pre-workshop record and still name `kits/`, `inventory/`, `KIT_TEMPLATE/` too). | Either mark `workshop-dev/outline.md` as v7-superseded in one header line, or produce v8 on `main` from this branch; the v7 text will mislead anyone who reads it as current. |
